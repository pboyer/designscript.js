{
  "name": "designscript.js",
  "version": "0.0.2",
  "description": "DesignScript in the browser",
  "repository": {
    "type": "git",
    "url": "git://github.com/pboyer/designscript.js.git"
  },
  "scripts": {
    "test": "make clean && make build && make test"
  },
  "license": "MIT",
  "devDependencies": {
  },
  "engines": {
    "node": ">=0.12.0"
  }
}
