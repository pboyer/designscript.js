a = 4;
b = a * 2;
a = 3;

builds a number node = 4
assigns the number node to the identifier node a

dep

builds a 2 node
builds a multiplication node
connects the 2 node to the multiplication node
connects the a node to the 

dep

set value of a to 3

dep

entering an associative block builds creates a new empty dep graph

4 => a => 
          * => 
2    => 

associative execution
replication
range syntax
classes
